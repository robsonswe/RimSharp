<UserControl x:Class="RimSharp.Views.Modules.Downloader.DownloaderView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
             xmlns:vm="clr-namespace:RimSharp.ViewModels.Modules.Downloader"
             d:DataContext="{d:DesignInstance Type=vm:DownloaderViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800"
             Background="{StaticResource RimworldBeigeBrush}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="300"/>
        </Grid.ColumnDefinitions>
        
        <!-- Navigation Bar -->
        <Border Grid.Row="0" Grid.Column="0" 
                Style="{StaticResource RimworldInfoHeaderBorder}"
                Margin="0,0,0,1">
            <StackPanel Orientation="Horizontal">
                <!-- Changed buttons to use the same style as Home button and removed conditional styling -->
                <Button Command="{Binding GoBackCommand}" 
                        Content="Back" 
                        Style="{StaticResource DownloaderButtonStyle}"/>
                <Button Command="{Binding GoForwardCommand}" 
                        Content="Forward" 
                        Style="{StaticResource DownloaderButtonStyle}"/>
                <Button Command="{Binding GoHomeCommand}" 
                        Content="Home" 
                        Style="{StaticResource DownloaderButtonStyle}"/>
            </StackPanel>
        </Border>
        
        <!-- WebView2 Control - IMPORTANT: Do not set Source property here -->
        <Border Grid.Row="1" Grid.Column="0"
                BorderBrush="{StaticResource RimworldBrownBrush}"
                BorderThickness="1">
            <wv2:WebView2 x:Name="SteamWorkshopBrowser" 
                          SourceChanged="SteamWorkshopBrowser_SourceChanged"/>
        </Border>
        
        <!-- Download Panel -->
        <Border Grid.Row="0" Grid.RowSpan="2" Grid.Column="1"
                Style="{StaticResource RimworldPanelBorder}"
                Margin="1,0,0,0">
            <DockPanel>
                <Border DockPanel.Dock="Top" 
                        Style="{StaticResource RimworldInfoHeaderBorder}">
                    <TextBlock Text="Mod Downloader" 
                               Foreground="{StaticResource RimworldWhiteBrush}" 
                               FontWeight="Bold" 
                               FontSize="16"/>
                </Border>
                
                <StackPanel DockPanel.Dock="Top" Margin="10">
                    <Button Content="Add Selected Mod" 
                            Command="{Binding AddModCommand}" 
                            Style="{StaticResource RimworldButtonStyle}" 
                            Margin="0,5"/>
                    <Button Content="Setup SteamCMD" 
                            Command="{Binding SetupSteamCmdCommand}" 
                            Style="{StaticResource RimworldButtonStyle}" 
                            Margin="0,5"/>
                    <Button Content="Check for Updates" 
                            Command="{Binding CheckUpdatesCommand}" 
                            Style="{StaticResource RimworldSaveButtonStyle}" 
                            Margin="0,5"/>
                    <Button Content="Download All" 
                            Command="{Binding DownloadCommand}" 
                            Style="{StaticResource RimworldRunButtonStyle}" 
                            Margin="0,5"/>
                </StackPanel>
                
                <Border DockPanel.Dock="Top" 
                        Style="{StaticResource RimworldListHeaderBorder}"
                        Margin="0,10,0,0">
                    <TextBlock Text="Download Queue" 
                               Foreground="{StaticResource RimworldWhiteBrush}" 
                               FontWeight="Medium"/>
                </Border>
                
                <ListBox DockPanel.Dock="Bottom" 
                         ItemsSource="{Binding DownloadList}" 
                         Style="{StaticResource RimworldListBox}"
                         Margin="0,1,0,0"
                         ScrollViewer.VerticalScrollBarVisibility="Auto">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Grid Margin="0,2">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>
                                
                                <TextBlock Grid.Row="0" 
                                           Text="{Binding Name}" 
                                           FontWeight="Bold" 
                                           Foreground="{StaticResource RimworldBrownBrush}"/>
                                
                                <!-- Custom ProgressBar with RimWorld style -->
                                <Border Grid.Row="1" 
                                        Height="15" 
                                        Margin="0,5" 
                                        Background="{StaticResource RimworldDarkBeigeBrush}" 
                                        BorderBrush="{StaticResource RimworldBrownBrush}" 
                                        BorderThickness="1">
                                    <Border HorizontalAlignment="Left" 
                                            Width="{Binding Progress, Converter={StaticResource MultiplyValueConverter}, ConverterParameter=.01}"
                                            Background="{StaticResource RimworldDarkGreenBrush}"/>
                                </Border>
                                
                                <TextBlock Grid.Row="2" 
                                           Text="{Binding Status}" 
                                           FontStyle="Italic" 
                                           FontSize="11" 
                                           Foreground="{StaticResource RimworldLightBrownBrush}"/>
                            </Grid>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </DockPanel>
        </Border>
        
        <!-- Status Bar -->
        <Border Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2"
                Background="{StaticResource RimworldLightBrownBrush}"
                BorderBrush="{StaticResource RimworldBrownBrush}"
                BorderThickness="0,1,0,0">
            <TextBlock Text="{Binding StatusMessage}" 
                       Padding="5" 
                       Foreground="{StaticResource RimworldWhiteBrush}"/>
        </Border>
    </Grid>
</UserControl>