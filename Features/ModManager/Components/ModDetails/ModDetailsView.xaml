<UserControl x:Class="RimSharp.Features.ModManager.Components.ModDetails.ModDetailsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:RimSharp.Features.ModManager.Components.ModDetails"
             xmlns:models="clr-namespace:RimSharp.Shared.Models"
             xmlns:vm="clr-namespace:RimSharp.Features.ModManager.ViewModels"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=vm:ModDetailsViewModel, IsDesignTimeCreatable=False}"
             d:DesignHeight="500" d:DesignWidth="400">
    
    <!-- DataContext should be a ModDetailsViewModel instance -->
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Mod Preview (Top Part) -->
        <Border Grid.Row="0"
                Margin="10"
                HorizontalAlignment="Stretch"
                VerticalAlignment="Top">
            <Border Background="{StaticResource RimworldDarkBrownBrush}"
                    CornerRadius="3"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    SnapsToDevicePixels="True"
                    MaxWidth="560" MaxHeight="308">
                <!-- Modified Image Container - Using Grid instead of Viewbox for better control -->
                <Grid Width="560" Height="308">
                    <!-- Bind to CurrentMod property -->
                    <Image Source="{Binding CurrentMod.PreviewImagePath, Converter={StaticResource ImagePathConverter}, TargetNullValue={x:Null}}"
                           Stretch="Uniform"
                           HorizontalAlignment="Center" 
                           VerticalAlignment="Center"
                           Width="Auto" 
                           Height="Auto"
                           MaxWidth="560" 
                           MaxHeight="308">
                        <Image.Style>
                            <Style TargetType="Image">
                                <Setter Property="Visibility" Value="Visible"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding Source, RelativeSource={RelativeSource Self}}" Value="{x:Null}">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Image.Style>
                    </Image>
                    <TextBlock Text="NO PREVIEW IMAGE" FontSize="18"
                               Foreground="{StaticResource RimworldLightBrownBrush}"
                               HorizontalAlignment="Center" VerticalAlignment="Center"
                               FontStyle="Italic"
                               Visibility="{Binding CurrentMod.PreviewImagePath, Converter={StaticResource BooleanToVisibilityConverter}, ConverterParameter=InvertIfNull}"/>
                </Grid>
            </Border>
        </Border>

        <!-- Mod Info (Bottom Part) -->
        <Grid Grid.Row="1" Margin="10">
            <!-- DataContext is the ModDetailsViewModel -->
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Mod Info Table -->
            <Grid Grid.Row="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/> <RowDefinition Height="Auto"/> <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/> <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.Resources>
                    <Style TargetType="TextBlock" x:Key="InfoLabel">
                        <Setter Property="FontWeight" Value="SemiBold"/>
                        <Setter Property="Margin" Value="0,0,10,2"/>
                        <Setter Property="VerticalAlignment" Value="Top"/>
                        <Setter Property="Foreground" Value="{StaticResource RimworldBrownBrush}"/>
                    </Style>
                    <Style TargetType="TextBlock" x:Key="InfoValue">
                        <Setter Property="TextWrapping" Value="Wrap"/>
                        <Setter Property="VerticalAlignment" Value="Top"/>
                        <Setter Property="Foreground" Value="{StaticResource RimworldBrownBrush}"/>
                        <Setter Property="Focusable" Value="True"/>
                    </Style>
                </Grid.Resources>

                <!-- Bind to CurrentMod property -->
                <TextBlock Grid.Row="0" Grid.Column="0" Text="Name:" Style="{StaticResource InfoLabel}"/>
                <TextBox Grid.Row="0" Grid.Column="1" 
                         Text="{Binding CurrentMod.Name, Mode=OneWay}" 
                         IsReadOnly="True"
                         BorderThickness="0"
                         Background="Transparent"
                         Foreground="{StaticResource RimworldBrownBrush}"
                         TextWrapping="Wrap"
                         VerticalAlignment="Top"/>

                <TextBlock Grid.Row="1" Grid.Column="0" Text="Authors:" Style="{StaticResource InfoLabel}"/>
                <TextBox Grid.Row="1" Grid.Column="1" 
                         Text="{Binding CurrentMod.Authors, Mode=OneWay}" 
                         IsReadOnly="True"
                         BorderThickness="0"
                         Background="Transparent"
                         Foreground="{StaticResource RimworldBrownBrush}"
                         TextWrapping="Wrap"
                         VerticalAlignment="Top"/>

                <TextBlock Grid.Row="2" Grid.Column="0" Text="PackageID:" Style="{StaticResource InfoLabel}"/>
                <TextBox Grid.Row="2" Grid.Column="1" 
                         Text="{Binding CurrentMod.PackageId, Mode=OneWay}" 
                         IsReadOnly="True"
                         BorderThickness="0"
                         Background="Transparent"
                         Foreground="{StaticResource RimworldBrownBrush}"
                         TextWrapping="Wrap"
                         VerticalAlignment="Top"/>

                <TextBlock Grid.Row="3" Grid.Column="0" Text="Supported:" Style="{StaticResource InfoLabel}"/>
                <!-- Keep TextBlock for versions but make it selectable with MouseBinding -->
                <Border Grid.Row="3" Grid.Column="1" Background="Transparent">
                    <ContentControl Content="{Binding CurrentMod.SupportedVersions, Converter={StaticResource VersionSupportConverter}}">
                        <ContentControl.Resources>
                            <Style TargetType="TextBlock">
                                <Setter Property="Focusable" Value="True"/>
                            </Style>
                        </ContentControl.Resources>
                    </ContentControl>
                </Border>

                <TextBlock Grid.Row="4" Grid.Column="0" Text="Path:" Style="{StaticResource InfoLabel}"/>
                <Grid Grid.Row="4" Grid.Column="1">
                    <TextBox Text="{Binding CurrentMod.Path, Mode=OneWay}"
                             IsReadOnly="True"
                             BorderThickness="0"
                             Background="Transparent"
                             Foreground="{StaticResource RimworldBrownBrush}"
                             TextWrapping="NoWrap"
                             VerticalAlignment="Top"
                             ToolTip="{Binding CurrentMod.Path}"/>
                    <!-- Hyperlink overlay for clicking -->
                    <Button Command="{Binding OpenUrlCommand}"
                            CommandParameter="{Binding CurrentMod.Path}"
                            Background="Transparent"
                            BorderThickness="0"
                            Cursor="Hand"
                            HorizontalAlignment="Stretch"
                            VerticalAlignment="Stretch"
                            Opacity="0"/>
                </Grid>
            </Grid>

            <!-- Mod Description -->
            <Border Grid.Row="1" Margin="0,10,0,0" BorderBrush="{StaticResource RimworldLightBrownBrush}" BorderThickness="0,1,0,0" Padding="0,10,0,0">
                <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                     <!-- Bind to CurrentMod property -->
                     <TextBox Text="{Binding CurrentMod.Description, Mode=OneWay}" 
                              TextWrapping="Wrap" 
                              Foreground="{StaticResource RimworldBrownBrush}"
                              IsReadOnly="True"
                              BorderThickness="0"
                              Background="Transparent"
                              VerticalScrollBarVisibility="Auto"/>
                </ScrollViewer>
            </Border>
        </Grid>
    </Grid>
</UserControl>